/* Configuração do app nativo.
 *
 * A chave pública do RevenueCat vai aqui. Ela é pública de propósito — o SDK
 * roda no aparelho, então qualquer um consegue lê-la. Quem NÃO pode vir para
 * cá é a secret key: ela vive só no servidor.
 *
 * Sem a chave preenchida o app funciona normalmente; só a tela de assinatura
 * avisa que a compra ainda não está disponível.
 */
window.RC_CHAVE_PUBLICA = 'appl_rvDzWnllpOOmfaHAKcbpDVYetps';
